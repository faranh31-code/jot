// Native (iOS/Android) re-export of expo-share-intent's hook. See the .web.ts
// sibling for the web-preview stub — expo-share-intent's native module can't
// be bundled for web at all, following the same pattern as ads.web.ts.
export { useShareIntent } from 'expo-share-intent';
